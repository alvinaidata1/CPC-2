name: RDP1

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:
      - name: Configure Core RDP Settings
        run: |
          Set-ItemProperty 'HKLM:\System\CurrentControlSet\Control\Terminal Server' `
            -Name fDenyTSConnections -Value 0 -Force

          Set-ItemProperty 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
            -Name UserAuthentication -Value 0 -Force

          netsh advfirewall firewall add rule name="RDP-Bore" dir=in action=allow protocol=TCP localport=3389
          Restart-Service TermService -Force

      - name: Create RDP User (SIMPLE PASSWORD WORKS)
        run: |
          $username = "${{ secrets.NAME }}"
          $password = "${{ secrets.PASS }}"

          # Disable password policy
          secedit /export /cfg $env:TEMP\secpol.cfg
          (Get-Content $env:TEMP\secpol.cfg) `
            -replace "PasswordComplexity = 1","PasswordComplexity = 0" `
            -replace "MinimumPasswordLength = \d+","MinimumPasswordLength = 0" |
            Set-Content $env:TEMP\secpol.cfg

          secedit /configure /db secedit.sdb /cfg $env:TEMP\secpol.cfg /areas SECURITYPOLICY
          gpupdate /force

          # Remove user if already exists
          net user $username /delete 2>$null

          # Create user (accepts simple passwords)
          net user $username $password /add
          net localgroup Administrators $username /add
          net localgroup "Remote Desktop Users" $username /add

          echo "RDP_USER=$username" >> $env:GITHUB_ENV
          echo "RDP_PASS=$password" >> $env:GITHUB_ENV

      - name: Clean Azure / Assure / Unity Shortcuts
        run: |
          Get-ChildItem "C:\Users\Public\Desktop" |
            Where-Object { $_.Name -match "Azure|Assure|Unity" } |
            Remove-Item -Force -Recurse -ErrorAction SilentlyContinue

          Get-ChildItem "C:\ProgramData\Microsoft\Windows\Start Menu\Programs" -Recurse |
            Where-Object { $_.Name -match "Azure|Assure|Unity" } |
            Remove-Item -Force -Recurse -ErrorAction SilentlyContinue

      - name: Install Bore
        run: |
          Invoke-WebRequest `
            https://github.com/ekzhang/bore/releases/download/v0.5.1/bore-v0.5.1-x86_64-pc-windows-msvc.zip `
            -OutFile bore.zip
          Expand-Archive bore.zip -DestinationPath C:\bore
          echo "C:\bore" >> $env:GITHUB_PATH

      - name: PRINT CONNECTION INFO (COPY THIS)
        run: |
          Write-Host ""
          Write-Host "============== CONNECT =============="
          Write-Host "BORE_PUB: ${{ secrets.BORE_SERVER }}:${{ secrets.BORE_PUBLIC_PORT }}"
          Write-Host ""
          Write-Host "mstsc /v:${{ secrets.BORE_SERVER }}:${{ secrets.BORE_PUBLIC_PORT }}"
          Write-Host ""
          Write-Host "Username: $env:RDP_USER"
          Write-Host "Password: $env:RDP_PASS"
          Write-Host "====================================="
          Write-Host ""

      - name: Start Bore TCP Tunnel
        run: |
          bore local 3389 `
            --to ${{ secrets.BORE_SERVER }}:${{ secrets.BORE_PORT }} `
            --port ${{ secrets.BORE_PUBLIC_PORT }}

      - name: Maintain Connection
        run: |
          while ($true) {
            Write-Host "[$(Get-Date)] RDP ACTIVE (BORE TCP)"
            Start-Sleep 300
          }
